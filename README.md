## PARTE A
1. B
2. B
3. B
4. B
5. B
6. B-
7. A
8. C
9. B
10. A
11. B
12. B
13. B
14. B
15. B 

## PARTE B
### Pregunta 1 
Lo que tengo entendido como algo algoritmo es que es como una serie de pares que son ordenados que se los hace para resolver un problema. Por lo cual lo primero sería entender el problema, luego pensar como puedo solucionarlo, despues escribir los pasos que tengo que seguir, para luego programarlo en el lenguaje, finalmente, probar si funciona y corregir errores que tenga. 
### Pregunta 2
Un puntero básicamente es la variable que guarda la dirección de otra variable. Los operadores * y &, el primero sirve para poder acceder al valor guardado en esa dirección y el segundo sirve para conseguir la dirección de la variable.
### Pregunta 3
Un arreglo vendría a ser un grupo de variables del mismo tipo que se las termina guardando juntas. Estos siempre comienzan por cero y la forma en la cual se declara es como por ejemplo int numeros[3]; y si queremos poder acceder a sus elementos es así numeros[0] = 10;
numeros[1] = 20;
### Pregunta 4
Un alcance (scope) de una variable es lo que se refiere a que es la parte del programa donde una variable podríamos usar y la diferencia entre una variable local y una global, es que la local es cuando la creamos dentro de la función y como que solo sirve ahí, pero la global es cuando se crea fuera de las funciones y podemos usarlas en todo el programa. 

### Pregunta 5 
Usamos private y public en una clase porque así podemos saber que partes de una clase se pueden usar desde afuera, por ejemplo la private solo la podemos usar variables o funciones, en cambio, la public cualquier parte del programa podemos usarlas. Por otro lado, el encapsulamiento significa que podemos proteger los datos para así poder evitar cambios incorrectos.









































